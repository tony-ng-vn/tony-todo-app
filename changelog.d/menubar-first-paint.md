**Web App**

- The menu bar popover and floating notes now open straight into your cached tasks instead of sitting on "Connecting..." until the server answers; the sign-in screen appears only once the session check has actually resolved signed out.
