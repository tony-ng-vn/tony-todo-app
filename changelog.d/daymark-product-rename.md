**Web App**

- The sign-in screen, menu bar heading, update copy, and agent setup now say Daymark.

**Native App**

- The macOS app installs as `/Applications/Daymark.app`, shows Daymark in menus and windows, and loads `https://daymark.inhavens.com`.
- Local menu bar runs still use a separate Daymark Local identity so they do not replace the installed app.

**Docs**

- README, the menu bar guide, and the native update runbook now use the Daymark name and install path.
