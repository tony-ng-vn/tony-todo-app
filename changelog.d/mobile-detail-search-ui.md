**Web App**

- Opening a task on a phone now fills the visible screen, keeps Close pinned, and still scrolls the notes when the keyboard is up.
- Search uses the same field surface as naming a new task, without the native blue focus box.
