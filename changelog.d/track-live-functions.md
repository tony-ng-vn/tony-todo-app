**Backend**

- The claim-preauth-todos and extract-video-knowledge functions are now tracked in the repository, so the deploy pipeline verifies their live source and CI pins their security behavior like every other function.
