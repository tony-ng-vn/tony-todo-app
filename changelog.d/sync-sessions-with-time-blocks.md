**Web App**

- Editing or deleting a time block now updates or removes the matching recap session, and those session changes sync to the cloud.
- Timer, completion, note, and time-block saves for the same task now reach the cloud in order, so a slower older save cannot restore stale timing.

**Native App**

- The menu bar uses the same time-block edits, so recap sessions stay in sync there too.
