{
  "name": "todo-day-summary",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "audit:dependencies": "npm audit --audit-level=high",
    "check:swift-toolchain": "node scripts/check-swift-toolchain.mjs",
    "test": "vitest run",
    "test:native-menubar": "swift test --package-path native",
    "test:ui": "node scripts/ui-smoke.mjs",
    "test:menubar": "node scripts/menubar-smoke.mjs",
    "build": "vite build",
    "build:native-menubar": "swift build --package-path native -c release",
    "dev": "vite dev --host 127.0.0.1",
    "menubar": "swift run --package-path native -c release done-log-menubar",
    "menubar:dev": "DONE_LOG_MENUBAR_URL=http://127.0.0.1:5176/menubar?local=1 swift run --package-path native done-log-menubar",
    "menubar:check": "DONE_LOG_MENUBAR_URL=http://127.0.0.1:5176/menubar?local=1 MENUBAR_NATIVE_SMOKE=1 swift run --package-path native done-log-menubar",
    "menubar:bundle": "bash scripts/build-menubar-app.sh",
    "menubar:install": "bash scripts/install-menubar-app.sh",
    "verify:web": "npm test && npm run build && npm run audit:dependencies",
    "verify:native": "npm run check:swift-toolchain && npm run test:native-menubar && npm run build:native-menubar && npm run menubar:bundle",
    "verify": "npm run verify:web && npm run verify:native",
    "understanding:build": "node scripts/understanding-workbook.mjs",
    "understanding:serve": "node scripts/understanding-workbook.mjs && node scripts/understanding-serve.mjs"
  },
  "devDependencies": {
    "@sveltejs/adapter-auto": "^7.0.1",
    "@sveltejs/kit": "^2.64.0",
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "playwright": "^1.60.0",
    "svelte": "^5.56.3",
    "vite": "^8.0.16",
    "vitest": "^4.1.8",
    "yaml": "^2.9.0"
  },
  "dependencies": {
    "@insforge/sdk": "^1.3.1",
    "@vercel/analytics": "^2.0.1",
    "@vercel/speed-insights": "^2.0.0",
    "feedback-sdk-widget": "^0.1.0",
    "lottie-web": "^5.13.0",
    "suncalc": "^2.0.1"
  },
  "overrides": {
    "cookie": "^0.7.2"
  }
}
